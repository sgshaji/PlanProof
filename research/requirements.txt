# PlanProof Research Module Dependencies
#
# Install: pip install -r research/requirements.txt
# Note: On Windows ARM64, use x64 Python (see docs/setup-local.md)

# Core graph + ML
networkx>=3.2
python-igraph>=0.11
leidenalg>=0.10
scikit-learn>=1.4
numpy>=1.26
matplotlib>=3.8
pandas>=2.1
pyarrow>=14.0

# Azure OpenAI + GraphRAG
openai>=1.10
graphrag>=3.0
python-dotenv>=1.0

# PDF extraction
pdfplumber>=0.11
PyMuPDF>=1.24

# Optional: Database (only needed if use_database=True)
# sqlalchemy>=2.0
# psycopg[binary]>=3.3

# Optional: IFC/BIM parsing
# ifcopenshell>=0.7
pyarrow>=14.0
