# Vite Frontend Environment Variables

# API Base URL - leave empty since API endpoints already include /api/v1 prefix
# The Vite dev proxy forwards /api/* requests to http://localhost:8000
# Setting this to /api would result in /api/api/v1/* (double prefix)
VITE_API_URL=
